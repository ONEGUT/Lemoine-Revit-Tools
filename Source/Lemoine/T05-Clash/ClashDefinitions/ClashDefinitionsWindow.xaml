<Window
    x:Class="LemoineTools.Lemoine.ClashDefinitionsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
    Title="Lemoine Tools — Clash Definitions"
    Width="1100" Height="700"
    MinWidth="860" MinHeight="500"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    ResizeMode="CanResizeWithGrip"
    AllowsTransparency="False"
    ShowInTaskbar="True">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="8"
                            GlassFrameThickness="0" UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Border x:Name="_outerBorder" BorderThickness="1" ClipToBounds="True">
        <Grid x:Name="_root">
            <Grid.RowDefinitions>
                <RowDefinition Height="38" />
                <RowDefinition Height="*"  />
            </Grid.RowDefinitions>

            <Border x:Name="_toolbarBorder" Grid.Row="0" ClipToBounds="True" />

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="280" />
                    <ColumnDefinition Width="*"   />
                </Grid.ColumnDefinitions>

                <Border x:Name="_sidebarBorder" Grid.Column="0" BorderThickness="0,0,1,0" />
                <Border x:Name="_editorBorder"  Grid.Column="1" />
            </Grid>
        </Grid>
    </Border>
</Window>
