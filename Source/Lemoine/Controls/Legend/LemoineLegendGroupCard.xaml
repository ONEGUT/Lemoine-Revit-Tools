<UserControl
    x:Class="LemoineTools.Lemoine.Controls.LemoineLegendGroupCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border x:Name="_outer" BorderThickness="0" CornerRadius="{DynamicResource LemoineRadius_Card}" Padding="0" MinWidth="240">
        <Grid x:Name="_root">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Border x:Name="_header" Grid.Row="0" Padding="6,4,6,4" />
            <Border x:Name="_body"   Grid.Row="1" Padding="8,4,8,8" />
        </Grid>
    </Border>
</UserControl>
