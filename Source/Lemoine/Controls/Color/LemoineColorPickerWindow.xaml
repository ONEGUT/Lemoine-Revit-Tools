<Window
    x:Class="LemoineTools.Lemoine.Controls.LemoineColorPickerWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
    Title="Pick Color"
    SizeToContent="WidthAndHeight"
    WindowStartupLocation="CenterOwner"
    WindowStyle="None"
    ResizeMode="NoResize"
    AllowsTransparency="False"
    ShowInTaskbar="False">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome
            CaptionHeight="0"
            ResizeBorderThickness="0"
            GlassFrameThickness="0"
            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Border x:Name="_outerBorder" BorderThickness="1">
        <Grid x:Name="_root">
            <Grid.RowDefinitions>
                <RowDefinition Height="38" />
                <RowDefinition Height="*"  />
                <RowDefinition Height="42" />
            </Grid.RowDefinitions>
            <Border x:Name="_toolbarBorder" Grid.Row="0" BorderThickness="0,0,0,1" />
            <Border x:Name="_bodyBorder"    Grid.Row="1" Padding="14,12,14,12">
                <ContentControl x:Name="_panelSlot" />
            </Border>
            <Border x:Name="_footerBorder"  Grid.Row="2" BorderThickness="0,1,0,0" Padding="{DynamicResource LemoineTh_FooterPad}" />
        </Grid>
    </Border>
</Window>
